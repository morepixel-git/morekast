# MoreKast

## Build sur Linux

Paquets nécessaires :

sudo apt update && sudo apt install -y clang cmake ninja-build pkg-config libgtk-3-dev libstdc++-12-dev liblzma-dev

flutter build linux --release
